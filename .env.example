# Backend bootstrap secrets — runtime config lives in DB (admin Settings page)
DATABASE_URL=postgresql+asyncpg://marathakalyanam:marathakalyanam@localhost:5432/marathakalyanam
SESSION_SECRET=change-me-to-32-random-bytes
MEDIA_ROOT=./backend/var/media

# SMTP — placeholders, real values entered via admin Settings UI later
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@marathakalyanam.com
OWNER_EMAIL=admin.marathakalyanam@gmail.com

# Frontend
PUBLIC_API_BASE_URL=http://localhost:8000
